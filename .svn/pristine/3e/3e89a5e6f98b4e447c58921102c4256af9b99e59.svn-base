using System;
using System.Collections.Generic;
using System.Data.Objects;
using System.Linq;
using System.Text;
//using EF;

namespace Server
{
   /* public class CustomerDataServices
    {
        private static CustomerDataServices instance;
        public static CustomerDataServices Instance
        {
            get { return instance ?? (instance = new CustomerDataServices()); }
        }
        private CustomerDataServices() { }

        public IEnumerable<CUSTOMER> GetAllCustomers()
        {
            using (var ctx = new LayersEntities())
            {
                return ctx.CUSTOMERS.ToList();
            }
        }

        public void Insert(CUSTOMER customer)
        {
            using (var ctx = new LayersEntities())
            {
                ctx.CUSTOMERS.AddObject(customer);
                ctx.SaveChanges();
            }
        }

    }*/
}
