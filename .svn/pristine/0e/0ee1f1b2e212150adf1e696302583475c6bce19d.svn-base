using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Entities
{
    public class Services
    {
        public Services(string servicesName, int idService)
        {
            this.servicesName = servicesName;
            this.IdService = idService;
        }

        public Services(string servicesName)
        {
            this.servicesName = servicesName;
        }

        public Services()
        { 
        
        }

        public Services(int idService)
        {
            this.IdService = idService;
        }

        public string servicesName { get; set; }
        public int IdService { get; set; }
           

    /*    public override string ToString()
        {
            var sb = new StringBuilder();
            sb.Append(string.Format("Name = {0}; ", this.Name));
            sb.Append(string.Format("Address = {0}; ", this.Address));
           // sb.AppendLine(string.Format("Id = {0}.", this.Id));
            return sb.ToString();

        }*/
    }
}
